// This is here so the directory is not empty.
// Otherwise typescript will not generate a dist/pkg directory and the build will fail.
// Once there is some actual code in this folder you can remove this file.

/**
 * Dummy function, see file header.
 */
const helloWorld = (): void => {
  console.log("Hello World")
}
