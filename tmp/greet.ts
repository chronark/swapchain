/**
 * Greets a user.
 *
 * @param name The name of someone you would like to greet.
 * @returns A greeting directed at the given name.
 */
export function greet(name: string): string {
  return `Hello ${name}!`
}
