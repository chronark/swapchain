export enum State {
  IDLE,
  INVALID_INPUT,
  RUNNING,
  SUCCESS,
  ERROR,
  FAILURE,
}

export enum Currency {
  BTC,
  BTS,
}

export enum Network {
  MAINNET = "mainnet",
  TESTNET = "testnet",
}

export enum Timelock {
  LONG,
  MEDIUM,
  SHORT,
}

export enum Priority {
  HIGH,
  MEDIUM,
  LOW,
}
